import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './dhamija_s_trading_dashboard' // Ensure the filename matches exactly
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
